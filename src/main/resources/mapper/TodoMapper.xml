<?xml version="1.0" encoding="UTF-8"?>
<!-- AI 生成 By Peng.Guo -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pengguo.myspringboot.infrastructure.repository.TodoMapper">

    <resultMap id="TodoEntityMap" type="com.pengguo.myspringboot.infrastructure.entity.TodoEntity">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="done" column="done"/>
        <result property="createdAtEpochSeconds" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO todos (title, done, created_at)
        VALUES (#{title}, #{done}, #{createdAtEpochSeconds})
    </insert>

    <select id="findAll" resultMap="TodoEntityMap">
        SELECT id, title, done, created_at
        FROM todos
        ORDER BY id
    </select>
</mapper>
